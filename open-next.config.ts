const openNextConfig = {
  default: {
    placement: "regional",
    runtime: "node"
  },
  dangerous: {
    disableIncrementalCache: true,
    disableTagCache: true
  }
};

export default openNextConfig;
